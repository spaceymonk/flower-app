import './CustomEdge.style.css';
import CustomEdge from './CustomEdge';
import CustomConnectionLine from './CustomConnectionLine';

export { CustomEdge, CustomConnectionLine };

export const edgeTypes = {
  custom: CustomEdge,
};
